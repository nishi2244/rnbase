
const initialState = {
  data: [],
  // dataFetched: false,
  isFetching: false,
  nextUrl: '',
  // error: false
};

export default function listReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
